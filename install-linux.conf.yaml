- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bash_profile: 'linux/bashrc.sh'
    ~/.bash_aliases_unix: 'unix/bash_aliases.sh'
    ~/.bash_aliases_unix: 'unix/bash_aliases_unix.sh'

- shell:
  - [git submodule update --init --recursive, Installing submodules]