- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.myconf: 'macos/myconf.conf'
    ~/.bash_profile: 'macos/bash_profile.sh'

- shell:
  - [git submodule update --init --recursive, Installing submodules]